# 🔧 حل مشكلة CORS في Google Apps Script

## المشكلة:
```
Access to fetch at 'https://script.google.com/macros/s/...' from origin 'https://amr2del.github.io' 
has been blocked by CORS policy
```

## السبب:
Google Apps Script لا يسمح بطلبات CORS من نطاقات مختلفة بشكل افتراضي.

---

## ✅ الحل:

### الخطوة 1: تحديث كود Google Apps Script

1. افتح Google Apps Script Editor
2. انسخ الكود المحدث من ملف `google-apps-script.gs`
3. استبدل الكود القديم بالجديد
4. احفظ المشروع

**التحديث الجديد يتضمن:**
- إضافة دالة `doOptions()` لمعالجة preflight requests

### الخطوة 2: إعادة نشر Web App

⚠️ **هام جداً:** يجب إعادة النشر بإصدار جديد

1. في Google Apps Script Editor، اضغط "Deploy" → "Manage deployments"
2. اضغط على أيقونة القلم ✏️ بجانب الـ deployment الحالي
3. في "Version"، اختر **"New version"**
4. أضف وصف: "إضافة دعم CORS"
5. اضغط "Deploy"
6. ✅ الآن Web App يدعم CORS

### الخطوة 3: تحديث ملف sheets-api.js (تم بالفعل)

الكود الآن يستخدم `mode: 'no-cors'` لتجنب مشاكل CORS.

---

## 🧪 اختبار:

1. افتح Console في المتصفح (F12)
2. جرب إضافة منتج جديد
3. يجب أن تظهر رسالة نجاح بدون أخطاء CORS
4. تحقق من Google Sheet أن البيانات تم حفظها

---

## ⚠️ ملاحظة هامة:

في وضع `no-cors`، لن نحصل على استجابة من السيرفر (response body)، لكن الطلبات ستصل إلى Google Sheets وستُحفظ البيانات بنجاح.

**الأفضل:** استخدام GET requests بدلاً من POST لأن Google Apps Script يدعم GET بشكل أفضل مع CORS.

---

## 🔄 طريقة بديلة (الأفضل):

### تغيير الطريقة من POST إلى GET مع parameters

هذه الطريقة أكثر موثوقية وتدعم CORS بشكل كامل.

سأقوم بتحديث الكود لاستخدام GET بدلاً من POST إذا استمرت المشكلة.

---

## 📝 خلاصة الخطوات:

1. ✅ حدثنا كود Google Apps Script
2. ✅ حدثنا sheets-api.js
3. ⏳ يجب عليك: إعادة نشر Web App بإصدار جديد
4. ⏳ يجب عليك: اختبار التطبيق
