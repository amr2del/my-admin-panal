# نظام إدارة قطع الغيار - دليل التشغيل

## 🚀 كيفية التشغيل

### 1. تثبيت المكتبات (مرة واحدة فقط)
```bash
npm install
```

### 2. تشغيل السيرفر
```bash
npm start
```

السيرفر سيعمل على: `http://localhost:3000`

### 3. فتح الموقع
افتح ملف `index.html` في المتصفح أو اذهب إلى:
```
http://localhost:3000/index.html
```

---

## 📁 هيكل المشروع

```
my admin/
├── index.html          # الواجهة الأمامية
├── style.css           # التنسيقات
├── app.js              # منطق التطبيق
├── api.js              # وظائف الاتصال بالباك اند
├── server.js           # سيرفر الباك اند (Node.js + Express)
├── database.json       # قاعدة البيانات (JSON)
└── package.json        # معلومات المشروع والمكتبات
```

---

## 🔌 API Endpoints

### المنتجات
- `GET /api/products` - جلب جميع المنتجات
- `POST /api/products` - إضافة منتج جديد
- `PUT /api/products/:id` - تحديث منتج
- `DELETE /api/products/:id` - حذف منتج

### المبيعات
- `GET /api/sales` - جلب جميع المبيعات
- `POST /api/sales` - إضافة عملية بيع جديدة
- `DELETE /api/sales/:id` - حذف عملية بيع

### الإعدادات
- `GET /api/settings` - جلب الإعدادات
- `PUT /api/settings` - تحديث الإعدادات

### نقل البيانات
- `POST /api/migrate` - نقل البيانات من LocalStorage إلى السيرفر

---

## ✨ المميزات

✅ **تخزين دائم للبيانات** - البيانات محفوظة في ملف `database.json`  
✅ **واجهة برمجية API** - يمكن الوصول للبيانات من أي جهاز على نفس الشبكة  
✅ **نقل تلقائي** - نقل البيانات الموجودة من LocalStorage إلى السيرفر  
✅ **سهل التطوير** - كود بسيط وواضح للتعديل والإضافة  

---

## 🔧 ملاحظات مهمة

1. **يجب تشغيل السيرفر أولاً** قبل فتح الموقع
2. **البيانات محفوظة** في ملف `database.json` - لا تحذف هذا الملف
3. **للوصول من أجهزة أخرى** على نفس الشبكة:
   - استخدم عنوان IP الجهاز بدلاً من localhost
   - مثال: `http://192.168.1.100:3000`
4. **النسخ الاحتياطي**: انسخ ملف `database.json` بشكل دوري

---

## 🛠️ التطوير

للتطوير مع إعادة التشغيل التلقائي:
```bash
npm run dev
```

---

## 📞 الدعم

في حالة وجود مشاكل:
1. تأكد من تشغيل السيرفر (`npm start`)
2. تحقق من أن المنفذ 3000 غير مستخدم
3. افتح Console في المتصفح لمشاهدة الأخطاء
